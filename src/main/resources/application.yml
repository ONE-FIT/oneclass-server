spring:
  application:
    name: oneclass

  datasource:
    url: jdbc:postgresql://localhost:5432/oneclass
    username: root
    password: Abcde090125!!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: abcb090125@gmail.com
    password: kim753107-   # Gmail 앱 비밀번호
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: "0123456789ABCDEF0123456789ABCDEF" # 32비트
  token-validity-in-seconds: 1209600 # 14일

