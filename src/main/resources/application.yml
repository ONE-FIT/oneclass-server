spring:
  application:
    name: oneclass

  datasource:
    url: jdbc:postgresql://localhost:5432/oneclass
    username: root
    password: Abcde090125!!

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect

jwt:
  secret: "0123456789ABCDEF0123456789ABCDEF" # 32??
  token-validity-in-seconds: 1209600 # 14?